import React from 'react'

/**
 * EXIT INTENT PLACEHOLDER
 * -----------------------
 * This is a non-functional UI placeholder for future implementation of 
 * exit-intent logic (triggering a quiz modal when the user's mouse leaves the viewport).
 */
export default function ExitIntentPlaceholder() {
  // Logic will be added here in the future
  return null
}

/**
 * FLOATING CTA (CSS ONLY)
 * -----------------------
 * To enable the floating CTA, add the following to your global CSS:
 * 
 * .floatingQuizCTA {
 *    position: fixed;
 *    bottom: 24px;
 *    right: 24px;
 *    background: var(--atp-gold);
 *    color: var(--atp-navy);
 *    padding: 12px 20px;
 *    border-radius: 40px;
 *    font-weight: 700;
 *    box-shadow: 0 4px 20px rgba(0,0,0,0.15);
 *    z-index: 999;
 *    display: flex;
 *    align-items: center;
 *    gap: 8px;
 *    text-decoration: none;
 *    transition: transform 0.2s;
 * }
 * 
 * .floatingQuizCTA:hover {
 *    transform: scale(1.05);
 * }
 */
